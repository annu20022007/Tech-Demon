# Development backend URL (used by `npm run dev`)
VITE_API_URL=http://localhost:8001/api

# Production build served by FastAPI - use relative path so
# the same origin hosts both front and back. You can also set
# this value via environment when building: `VITE_API_URL=/api`
# VITE_API_URL=/api